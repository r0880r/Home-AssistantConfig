# https://home-assistant.io/components/recorder/
db_url: !secret db_url
purge_interval: 1
purge_keep_days: 7
exclude:
  domains:
    - automation
    - weblink
    - camera
    - group
  entities:
    - sensor.time
#include:
#  entities: !include recording_entities.yaml

